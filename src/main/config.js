const config = {
    ethereumNodeUrl: 'https://mainnet.infura.io/v3/YOUR_INFURA_PROJECT_ID', // Replace with your Infura project ID or local node URL
    networkId: '1', // Mainnet ID; change to '3' for Ropsten, '4' for Rinkeby, etc.
};

module.exports = config;
